const character = "#";
const count = 8;
const rows = [];

fundtion padRow(rownNumber, rowCount) {  
    return character.repeat(rowNumber);
}
for (let i = 0; i < 0; i = i + 1) {
    rows.push(padRow(i + 1, count));
}
